const LandingLayout = ({ children }) => (
  <div className=" flex flex-col items-center px-2  sm:px-4  bg-[var(--color-bg)] text-[var(--text-color)]">
    <div className="">{children}</div>
  </div>
);

export default LandingLayout;
